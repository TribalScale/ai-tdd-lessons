
describe('OOP Counter', () => {
  it('increments value', () => {
    expect(1).toBe(1);
  });
});

describe('Functional increment', () => {
  it('returns incremented value', () => {
    expect(1).toBe(1);
  });
});
